# API Configuration
#
# For local development with Vite proxy (recommended):
# Leave empty - Vite will proxy /api requests to backend on localhost:8080
VITE_API_BASE_URL=
#
# For production builds or connecting to remote API:
# Set to full backend URL (e.g., https://api.example.com)
# VITE_API_BASE_URL=https://api.example.com
